# Run scripts (lint, check, build) without the pre-run deps-status check that
# pnpm 11 added; it can fail on an unbuilt optional binary even though the tools
# work, and it is redundant in CI where install runs immediately beforehand.
verify-deps-before-run=false
